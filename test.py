import time

import utils
import cv2
import numpy as np
import pyautogui
import config

time.sleep(5)
# # template=cv2.imread('img/blue.png')
# # image = cv2.cvtColor(np.asarray(pyautogui.screenshot(region=config.config['card_pool_region'])), cv2.COLOR_RGB2BGR)
# # utils.show_match_image(template,  image)